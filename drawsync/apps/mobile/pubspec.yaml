name: drawsync_mobile
description: DrawSync mobile drawing controller
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_models:
    path: ../../packages/shared_models
  shared_protocol:
    path: ../../packages/shared_protocol
  shared_canvas:
    path: ../../packages/shared_canvas
  uuid: ^4.0.0

flutter:
  uses-material-design: true
