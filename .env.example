# Example .env file for AudioHub application
# "mysql://audiohub-user:your_password@localhost:3306/audiohub"
DATABASE_URL=your_database_url_here
PORT=8080
# Secret key for JWT authentication (replace with a secure random string in production)
JWT_SECRET=random_secret
